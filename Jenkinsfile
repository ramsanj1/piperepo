pipeline{
    agent any
    stages{
        stage ("build")
        {
            echo " build a project."
            
        }
        stage ("test")
        {
            echo "testing the project"
        }
    }
}
